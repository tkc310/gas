export type TToken = {
  contractAddress: string;
  name: string;
  symbol: string;
  createdAt: string;
  locked: string;
  code: string;
};

export type TIgnoreToken = {
  name: string;
  symbol: string;
};
